import { createContext } from "react";

// creating a new global context that components can write or read, no default value

/** 
 * Creates a empty react context
 */ 
export const UserContext = createContext()